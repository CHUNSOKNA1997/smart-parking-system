{
    "name": "blockchain",
    "version": "1.0.0",
    "description": "Smart contract for Smart Parking System payment records",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "node": "hardhat node",
        "deploy:local": "hardhat run scripts/deploy.ts --network localhost"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0",
        "typescript": "^5.3.0",
        "ts-node": "^10.9.2",
        "@types/node": "^20.10.0"
    },
    "dependencies": {
        "ethers": "^6.9.0"
    }
}
